function run(a)
  local page = http.get("http://pastebin.com/raw/" .. a)
  if fs.exists(a) then
    fs.delete(a)
  end
  h = fs.open(a, "w")
  h.write(page.readAll())
  h.close()
  shell.run(a)
  fs.delete(a)
end

settings.load("settings")
run(settings.get("pid"))
